# Generated by scripts/generate-publish-caller.mjs; review changes before merging.
name: Publish verified npm beta
on:
  workflow_dispatch:
    inputs:
      release-id:
        description: 'Numeric ID of the staged draft prerelease'
        required: true
        type: string
      version:
        description: 'Reviewed beta version staged as draft release vVERSION'
        required: true
        type: string
      source-sha:
        description: 'Full tested commit SHA; must be current main'
        required: true
        type: string
      artifact-sha256:
        description: 'SHA-256 of the exact independently verified candidate.tgz'
        required: true
        type: string
permissions: {}
jobs:
  publish:
    if: github.repository == 'jdorado/ez-voice' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      checks: read
      actions: read
      pull-requests: read
      id-token: write
    uses: jdorado/ez-agents/.github/workflows/npm-beta-shared.yml@bcd5c78de79d4d5249504fbf2a53d7a6567cefad
    with:
      package: '@jc_stack/ez-voice'
      publisher-sha: 'bcd5c78de79d4d5249504fbf2a53d7a6567cefad'
      required-checks: '["verify (22)","verify (24)","docker"]'
      release-id: ${{ inputs.release-id }}
      version: ${{ inputs.version }}
      source-sha: ${{ inputs.source-sha }}
      artifact-sha256: ${{ inputs.artifact-sha256 }}
